export enum GetAffiliateType {
  GET_ALL = 1,
  GET_LAST_30_DAYS = 2,
  GET_LAST_7_DAYS = 3,
  GET_TODAY = 4,
}

export enum AccountType {
  LIVE = 1,
  DEMO = 0,
}

export enum CommissionType {
  TRADING_COMMISSION = 1,
  VIP_COMMISSION = 2,
}


export enum UpVip {
  value = 100
}